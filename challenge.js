"use strict";
/*
const avrege = (a, b, c) => (a + b + c) / 3;

let Dolphins_points = avrege(44, 23, 71);
let Koals_points = avrege(65, 54, 49);
console.log(Dolphins_points, Koals_points);

function check_winner(avgDolphons, avgKoals) {
    if (avgDolphons >= 2 * avgKoals) {
        console.log(`Dlophins win🏆 ${avgDolphons} vs. ${avgKoals}`);
    } else if (avgKoals >= 2 * avgDolphons) {
        console.log(`Koals win🏆 ${avgKoals} vs. ${avgDolphons}`);
    } else {
        console.log('No one wins');
    }
}

check_winner(Dolphins_points, Koals_points);

check_winner(2334, 348758);


// Test 2
Dolphins_points = avrege(85, 54, 41);
Koals_points = avrege(23, 34, 27);
console.log(Dolphins_points, Koals_points);
check_winner(Dolphins_points, Koals_points);
*/

// Challenge 2
/*
const calcTip = function (bill) {
    return bill >= 50 && bill <= 300 ? bill * 0.15 : bill * 0.2;
}
const bills = [125, 555, 44];
const tip = [calcTip(bills[0]), calcTip(bills[1]), calcTip(bills[2])];
const totals = [bills[0] + tip[0], bills[1] + tip[1], bills[2] + tip[2]];
console.log(bills, tip, totals);
*/